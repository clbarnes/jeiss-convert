[build-system]
requires = ["setuptools>=45", "wheel", "setuptools_scm[toml]>=6.2"]

[tool.setuptools_scm]
write_to = "jeiss_convert/version.py"

[tool.isort]
profile = "black"
multi_line_output = 3

[tool.black]
extend-exclude = "^jeiss_convert/jeiss-specs/.*"
